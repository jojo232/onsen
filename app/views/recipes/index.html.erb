<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        温泉一覧
      </h1>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-multiline">
      <% @recipes.each do |recipe| %>
        <div class="">
          <div class="card">
            <div class="card-image">
              <figure class="">
                <%= link_to recipe_path(recipe) do %>
                  <%= attachment_image_tag recipe, :image, format: 'jpeg', size: "100x100" %>
                <% end %>
              </figure>
            </div>
            <div class="card-content">
              <div class="media">
                <div class="media-left">
                  <figure class="">
                    <%= attachment_image_tag recipe.user, :profile_image, format: 'jpeg', size: "100x100" , fallback: "no-image.png", class: "profile_image" %>
                  </figure>
                </div>
                <div class="media-content">
                  <div class="title"><%=link_to recipe.user.username, user_path(recipe.user) %></div>
                </div>
              </div>

              <div class="content">
                <time><%= recipe.updated_at.strftime("%Y-%m-%d %H:%M") %></time>更新
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</section>